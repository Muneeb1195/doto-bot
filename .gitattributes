# M1 market-data CSVs are NOT stored in git (previously git-lfs). They are
# exported on the home-server and uploaded as individual assets to a data-*
# GitHub release by scripts/push_data.py; train.yml and the optimize.yml
# shards download the assets they need via gh release download. H1/M15 CSVs
# stay in plain git.
