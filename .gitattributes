# H1/M15 history CSVs are ~41 MB total and commit fine as plain files.
# Only M1 (~90 MB per symbol) needs Git LFS, and it is not committed today.
data/history/*_M1.csv filter=lfs diff=lfs merge=lfs -text
